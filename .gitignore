# Repo-root ignores. Apply to the whole tree, including Academic-Gate_hp/.
# (Academic-Gate_hp/.gitignore repeats these so the site folder is self-contained
#  if it is ever extracted into its own repository.)

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Claude Code local settings (machine-specific; not shared)
.claude/

# Editors
.vscode/
.idea/
*.swp

# Logs / scratch
*.log
